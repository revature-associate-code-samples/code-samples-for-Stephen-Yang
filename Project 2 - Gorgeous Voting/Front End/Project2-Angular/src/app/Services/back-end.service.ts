import { Injectable } from '@angular/core';

@Injectable()
export class BackEndService {

  constructor() { }

  // restItemsServiceGetRestItems(restItemsUrl: string) {
  //   return this.http.get<any[]>(restItemsUrl).pipe(map(data => data));
  // }

}
